spring.application.name=EVENT-SERVICE
server.port=8082

# Eureka
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${server.port}

# DB (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3307/eventdb?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Disable Spring Cloud Config (you don't have a config server)
spring.cloud.config.enabled=false
spring.cloud.config.import-check.enabled=false

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=bouazizi.mohamedmahdi@esprit.tn
spring.mail.password=ubcw ywnr wcjh oick
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Monitoring (Actuator + Prometheus)
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.prometheus.enabled=true
management.prometheus.metrics.export.enabled=true

package com.esprit.demo.entity;

/**
 * Enum representing the different sections of an evaluation
 */
public enum QuestionSection {
    GRAMMAR,    // Grammar questions with correct answers
    LISTENING,  // Listening comprehension questions
    ESSAY       // Essay questions (manually graded)
}

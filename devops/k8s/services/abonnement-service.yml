# ============================================================
#  Abonnement Service - Kubernetes Deployment & Service
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: abonnement-service
  namespace: wordly
  labels:
    app: abonnement-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: abonnement-service
  template:
    metadata:
      labels:
        app: abonnement-service
        tier: backend
    spec:
      containers:
        - name: abonnement-service
          image: marwenazouzi1/abonnement-service:latest
          ports:
            - containerPort: 8084
              name: http
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://mysql:3306/abonnement_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
            - name: SPRING_DATASOURCE_USERNAME
              value: root
            - name: SPRING_DATASOURCE_PASSWORD
              value: root
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: http://eureka-server:8761/eureka/
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: abonnement-service
  namespace: wordly
  labels:
    app: abonnement-service
spec:
  type: ClusterIP
  selector:
    app: abonnement-service
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
      name: http

# ============================================================
#  Recrutement Service - Kubernetes Deployment & Service
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recrutement-service
  namespace: wordly
  labels:
    app: recrutement-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recrutement-service
  template:
    metadata:
      labels:
        app: recrutement-service
        tier: backend
    spec:
      containers:
        - name: recrutement-service
          image: YOUR_ACTUAL_USERNAME/recrutement-service:latest
          ports:
            - containerPort: 8093
              name: http
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://mysql:3306/recrutement_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
            - name: SPRING_DATASOURCE_USERNAME
              value: root
            - name: SPRING_DATASOURCE_PASSWORD
              value: root
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: http://eureka-server:8761/eureka/
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8093
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8093
            initialDelaySeconds: 30
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: recrutement-service
  namespace: wordly
  labels:
    app: recrutement-service
spec:
  type: ClusterIP
  selector:
    app: recrutement-service
  ports:
    - port: 8093
      targetPort: 8093
      protocol: TCP
      name: http
